## 修复
- 移除 macOS 外链打开路径中不稳定的 Shell 插件依赖，普通外链改由原生命令处理。
- RAG 删除和同名文档覆盖改为后台合并整理 LanceDB，减少前台阻塞。
- RAG 单条与批量更新、删除在部分失败后仍会刷新文档列表，避免显示陈旧状态。

## 兼容性
- 本版本仅生成 macOS Apple Silicon（arm64）安装包与自动更新产物。
- 发布产物使用既有更新签名配置；签名密钥轮换需作为独立的兼容性迁移处理。
